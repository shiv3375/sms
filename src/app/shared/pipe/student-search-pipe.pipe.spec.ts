import { StudentSearchPipePipe } from './student-search-pipe.pipe';

describe('StudentSearchPipePipe', () => {
  it('create an instance', () => {
    const pipe = new StudentSearchPipePipe();
    expect(pipe).toBeTruthy();
  });
});
